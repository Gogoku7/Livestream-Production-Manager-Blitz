@if (FormatSelection.SelectedFormat == null) return;

@if (FormatSelection.SelectedFormat == Enums.FormatsEnum.Singles)
{
    <LPMBlitz.FG.Components.SetForms.ManageSinglesSet Singles="Singles"></LPMBlitz.FG.Components.SetForms.ManageSinglesSet>
}
else if (FormatSelection.SelectedFormat == Enums.FormatsEnum.Doubles)
{
    <LPMBlitz.FG.Components.SetForms.ManageDoublesSet></LPMBlitz.FG.Components.SetForms.ManageDoublesSet>
}